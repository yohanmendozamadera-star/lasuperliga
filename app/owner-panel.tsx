"use client";
/* eslint-disable @next/next/no-html-link-for-pages, @typescript-eslint/no-explicit-any, react-hooks/exhaustive-deps */

import { FormEvent, useEffect, useMemo, useState } from "react";
import type { User } from "@supabase/supabase-js";
import { getSupabaseBrowserClient } from "../lib/supabase/client";
import "./platform.css";
import "./platform-overrides.css";

type League={id:string;name:string;slug:string;contact_name:string|null;contact_phone:string|null;subdomain_requests:{status:string}|null};
type Tournament={id:string;name:string;slug:string;status:string;start_date:string;format:string;players_on_field:number;qualifiers:number};
const slugify=(value:string)=>value.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"").slice(0,48);

export function OwnerPanel(){
 const supabase=useMemo(()=>getSupabaseBrowserClient(),[]);
 const [user,setUser]=useState<User|null>(null),[leagues,setLeagues]=useState<League[]>([]),[selected,setSelected]=useState(""),[tournaments,setTournaments]=useState<Tournament[]>([]);
 const [loading,setLoading]=useState(true),[showCreate,setShowCreate]=useState(false),[saving,setSaving]=useState(false),[message,setMessage]=useState("");
 const loadTournaments=async(orgId:string)=>{if(!supabase||!orgId){setTournaments([]);return;}const {data}=await supabase.from("tournaments").select("id,name,slug,status,start_date,format,players_on_field,qualifiers").eq("organization_id",orgId).order("created_at",{ascending:false});setTournaments((data??[]) as Tournament[]);};
 const loadAccount=async(current:User|null)=>{setUser(current);if(!supabase||!current){setLeagues([]);setLoading(false);return;}const {data}=await supabase.from("organizations").select("id,name,slug,contact_name,contact_phone,subdomain_requests(status)").eq("owner_id",current.id).order("created_at",{ascending:true});const owned=(data??[]).map((x:any)=>({...x,subdomain_requests:Array.isArray(x.subdomain_requests)?x.subdomain_requests[0]??null:x.subdomain_requests})) as League[];setLeagues(owned);const first=owned[0]?.id??"";setSelected(first);await loadTournaments(first);setLoading(false);};
 useEffect(()=>{if(!supabase){setLoading(false);return;}supabase.auth.getSession().then(({data})=>loadAccount(data.session?.user??null));const {data}=supabase.auth.onAuthStateChange((_event,session)=>loadAccount(session?.user??null));return()=>data.subscription.unsubscribe();},[supabase]);
 const changeLeague=async(id:string)=>{setSelected(id);setLoading(true);await loadTournaments(id);setLoading(false);};
 const createTournament=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();if(!supabase||!user||!selected)return;setSaving(true);setMessage("");const f=new FormData(e.currentTarget),name=String(f.get("name")||"").trim(),slug=slugify(String(f.get("slug")||name));const league=leagues.find(x=>x.id===selected);const {error}=await supabase.from("tournaments").insert({organization_id:selected,owner_id:user.id,name,slug,start_date:String(f.get("startDate")),format:String(f.get("format")),next_stage_format:String(f.get("nextStage")),registration_fee:Number(f.get("fee")||0),players_on_field:Number(f.get("players")||11),qualifiers:Number(f.get("qualifiers")||4),description:String(f.get("description")||""),contact_name:league?.contact_name||user.user_metadata?.full_name||"Organizador",contact_phone:league?.contact_phone||null,contact_email:user.email||null,status:"registration",is_public:true});if(error){setMessage(error.code==="23505"?"Ese nombre o enlace de torneo ya está en uso.":error.message);setSaving(false);return;}await loadTournaments(selected);setSaving(false);setShowCreate(false);};
 if(loading)return <main className="realPanelState"><img src="/liguita-logo-google-white.png" alt=""/><h1>Cargando tu espacio…</h1></main>;
 if(!user)return <main className="realPanelState"><img src="/liguita-logo-google-white.png" alt="Liguita"/><h1>Inicia sesión para continuar</h1><p>El panel administrativo solo muestra la información de tu propia liga.</p><a className="primaryBtn" href="/">Ir a iniciar sesión</a></main>;
 if(!leagues.length)return <main className="realPanelState"><img src="/liguita-logo-google-white.png" alt="Liguita"/><span className="sectionLabel">CUENTA CREADA</span><h1>Registra primero tu liga</h1><p>Aún no tienes campeonatos ni equipos. Completa los datos de tu organización y elige el subdominio que deseas.</p><a className="primaryBtn" href="/">Registrar mi liga y subdominio →</a></main>;
 const league=leagues.find(x=>x.id===selected)??leagues[0];
 return <main className="realPanel"><aside><a className="realPanelBrand" href="/"><img src="/liguita-logo-google-white.png" alt=""/>LIGUITA</a><p>PANEL DEL ORGANIZADOR</p><nav><a className="active" href="/panel">Torneos</a><a href={`/organizaciones/${league.slug}`}>Página pública</a></nav><small>{user.email}</small><button onClick={()=>supabase?.auth.signOut().then(()=>location.href="/")}>Cerrar sesión</button></aside><section><header><div><span className="sectionLabel">MI ORGANIZACIÓN</span><h1>{league.name}</h1><p><b>{league.slug}.liguita.co</b> · {league.subdomain_requests?.status==="active"?"Subdominio activo":"Pendiente de activación"}</p></div>{leagues.length>1&&<select value={selected} onChange={e=>changeLeague(e.target.value)}>{leagues.map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select>}</header><div className="panelTitle"><div><span className="sectionLabel">CAMPEONATOS</span><h2>Mis torneos</h2></div><button className="primaryBtn" onClick={()=>setShowCreate(true)}>+ Crear torneo</button></div>{tournaments.length?<div className="realTournamentGrid">{tournaments.map(t=><article key={t.id}><span className={`tournamentState ${t.status}`}>{t.status==="registration"?"Inscripciones":t.status.replace("_"," ")}</span><h3>{t.name}</h3><p>Inicia {new Date(`${t.start_date}T12:00:00`).toLocaleDateString("es-CO")} · {t.players_on_field} jugadores</p><small>{t.format==="round_robin"?"Todos contra todos":t.format} · {t.qualifiers} clasifican</small><div><a className="outlineBtn" href={`/organizaciones/${league.slug}/torneos/${t.slug}`}>Ver público</a></div></article>)}</div>:<div className="realEmpty"><b>⚽</b><h3>Aún no has creado torneos</h3><p>Este espacio está vacío porque tu cuenta es nueva. Crea el primer campeonato para comenzar.</p><button className="primaryBtn" onClick={()=>setShowCreate(true)}>Crear mi primer torneo →</button></div>}</section>{showCreate&&<div className="createOverlay"><form className="createTournament" onSubmit={createTournament}><button type="button" className="modalClose" onClick={()=>setShowCreate(false)}>×</button><p className="sectionLabel">NUEVO CAMPEONATO</p><h2>Crear torneo</h2><label>Nombre del torneo<input name="name" required placeholder="Copa La Playita Apertura 2026"/></label><label>Enlace corto<input name="slug" placeholder="copa-la-playita-2026"/></label><div className="formPair"><label>Fecha de inicio<input name="startDate" type="date" required/></label><label>Jugadores en cancha<select name="players" defaultValue="11"><option>6</option><option>9</option><option>11</option></select></label></div><div className="formPair"><label>Formato<select name="format" defaultValue="round_robin"><option value="round_robin">Todos contra todos</option><option value="groups">Fase de grupos</option></select></label><label>Después de clasificar<select name="nextStage" defaultValue="knockout"><option value="knockout">Eliminación directa</option><option value="round_robin">Todos contra todos</option></select></label></div><div className="formPair"><label>Equipos que clasifican<input name="qualifiers" type="number" min="1" defaultValue="4"/></label><label>Valor por equipo<input name="fee" type="number" min="0" defaultValue="0"/></label></div><label>Descripción breve<textarea name="description" rows={3}/></label>{message&&<div className="formMessage">{message}</div>}<button className="primaryBtn wide" disabled={saving}>{saving?"Guardando…":"Guardar torneo"}</button></form></div>}</main>;
}
